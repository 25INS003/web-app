import { PasswordResetFlow } from "@/features/auth/PasswordResetFlow";

export const metadata = { title: "Reset password · Nedyway" };

export default function ForgotPasswordPage() {
  return <PasswordResetFlow />;
}
